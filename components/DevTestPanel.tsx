// Production stub — the developer test panel has been removed from the public
// app. Kept as a no-op export to preserve back-compat with any stray imports.
export function DevTestPanel(_props: { defaultOpen?: boolean }): null {
  return null;
}
