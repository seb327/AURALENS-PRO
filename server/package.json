{
  "name": "auralens-ai-buddy-server",
  "version": "1.0.0",
  "private": true,
  "description": "Railway-deployable HTTP server for the AuraLens AI Aura Buddy. Mirrors the Supabase Edge Function pipeline.",
  "type": "module",
  "scripts": {
    "build": "esbuild src/index.ts --bundle --platform=node --target=node20 --format=esm --outfile=dist/index.js --banner:js=\"import { createRequire } from 'module'; const require = createRequire(import.meta.url);\" --packages=external",
    "start": "node dist/index.js",
    "dev": "tsx watch src/index.ts",
    "typecheck": "tsc --noEmit"
  },
  "dependencies": {
    "@supabase/supabase-js": "^2.106.2",
    "express": "^4.21.2",
    "stripe": "^17.5.0"
  },
  "devDependencies": {
    "@types/express": "^4.17.21",
    "@types/node": "^20.14.0",
    "esbuild": "^0.24.0",
    "tsx": "^4.19.2",
    "typescript": "~5.3.3"
  },
  "engines": {
    "node": ">=20"
  }
}
